# irori writes this at registration with a random scope identifier.
# Committing it makes a second KB from this template unregisterable and aborts
# incoming pulls. See docs/decisions/001-kb-structure.md (D10).
/.irori/scope.json

# The exchange surface. irori mounts cloud folders here; nothing is tracked.
/contents/

# Local development-agent artifacts
.local/agents/
